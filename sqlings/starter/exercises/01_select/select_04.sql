/*

I hope you are starting to get the hang of how you can select data from existing
tables. Now, let's already dive into how we can extract important information
from the data in the tables. Lets have a look at how you can select unique
values from a column.

Use the select statement to select unique (hint: distinct) values for the
'year_built' column in the aircraft table.

*/

select null;
